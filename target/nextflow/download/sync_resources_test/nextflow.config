  docker.enabled = true
  def viash_temp = System.getenv("VIASH_TEMP") ?: "/tmp/"
  docker.runOptions = "-i -v ${baseDir}:${baseDir} -v $viash_temp:$viash_temp"
  process.container = "dataintuitive/viash"
  params {
    sync_resources_test__input = "s3://czbiohub-pipelines/resources_test_ms"
    sync_resources_test__output = "resources_test"
    sync_resources_test__dryrun = "no_default_value_configured"
    id = ""
    testScript = "run_test.sh"
    testResources = [ "run_test.sh" ]
    sync_resources_test {
      name = "sync_resources_test"
      container = "download_sync_resources_test"
      containerTag = "0.2.0"
      containerRegistry = "ghcr.io"
      containerOrganization = "czbiohub/mspipelines"
      command = "sync_resources_test"
      tests {
        isDefined = true
        testScript = "run_test.sh"
        testResources = [ "run_test.sh" ]
      }
      arguments {
        input {
          name = "input"
          otype = "--"
          required = false
          type = "string"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.sync_resources_test__input}"
          dflt = "s3://czbiohub-pipelines/resources_test_ms"
          description = "Path to the S3 bucket to sync from."
        }
        output {
          name = "output"
          otype = "--"
          required = false
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.sync_resources_test__output}"
          dflt = "resources_test"
          description = "Path to the test resource directory."
        }
        dryrun {
          name = "dryrun"
          otype = "--"
          required = false
          type = "boolean_true"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.sync_resources_test__dryrun}"
          description = "Displays the operations that would be performed using the specified command without actually running them."
        }
      }
    }
  }