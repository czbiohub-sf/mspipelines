  docker.enabled = true
  def viash_temp = System.getenv("VIASH_TEMP") ?: "/tmp/"
  docker.runOptions = "-i -v ${baseDir}:${baseDir} -v $viash_temp:$viash_temp"
  process.container = "dataintuitive/viash"
  params {
    thermo_to_mzml__input = "viash_no_value"
    thermo_to_mzml__output = "viash_no_value"
    id = ""
    testScript = ""
    testResources = [  ]
    thermo_to_mzml {
      name = "thermo_to_mzml"
      container = "convert_thermo_to_mzml"
      containerTag = "main_build"
      containerRegistry = "ghcr.io"
      containerOrganization = "czbiohub/mspipelines"
      command = "thermo_to_mzml"
      tests {
        isDefined = false
        testScript = "NA"
        testResources = [  ]
      }
      arguments {
        input {
          name = "input"
          otype = "--"
          required = true
          type = "file"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.thermo_to_mzml__input}"
          example = "input.raw"
          description = "A Thermo Raw file."
        }
        output {
          name = "output"
          otype = "--"
          required = true
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.thermo_to_mzml__output}"
          example = "output.mzML"
          description = "Path to an .mzML file."
        }
      }
    }
  }